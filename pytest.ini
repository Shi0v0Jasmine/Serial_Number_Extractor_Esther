[pytest]
minversion = 9.0
addopts = -ra -q --strict-markers
testpaths = tests
markers =
    integration: generated PDF and workbook integration tests
    real_pdf: local commercial PDF regression tests, skipped unless configured
    ocr_integration: PaddleOCR integration tests
